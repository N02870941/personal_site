/*
 * to finish creating a page that can be reached from the header
 * create a component with the following naming convention 'jdComponentName'
 */
app.component('jdExample', {
  template: "<h1>DOGS!</h1>"
})
